# SQL-Data-Analysis-Queries---Miuul
Mastered SQL query writing to extract business insights from transactional datasets. Utilized JOINs, GROUP BY, HAVING, subqueries, and window functions like ROW_NUMBER() to analyze revenue per customer, product categories, and top-selling items per country. Transformed raw operational data into actionable analytics logic.
